# Experiment config: case118 single-case run (compute).
extends: ../config.yaml

run_tests: 0

logging:
  run_dir_mode: overwrite
  run_name: case118

compute:
  input: data/input/pglib_opf_case118_ieee.m
  slack_bus: 0
  export_results: verification/results/case118.json