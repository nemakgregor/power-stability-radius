# Experiment config: case1354_pegase single-case run (compute).
extends: ../config.yaml

run_tests: 0

logging:
  run_dir_mode: overwrite
  run_name: case1354_pegase

compute:
  input: data/input/pglib_opf_case1354_pegase.m
  slack_bus: 0
  export_results: verification/results/case1354_pegase.json