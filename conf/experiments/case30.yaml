# Experiment config: case30 single-case run (compute).
extends: ../config.yaml

run_tests: 0

logging:
  run_dir_mode: overwrite
  run_name: case30

compute:
  input: data/input/pglib_opf_case30_ieee.m
  slack_bus: 0
  export_results: verification/results/case30.json