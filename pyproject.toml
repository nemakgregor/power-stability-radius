[tool.poetry]
name = "stability-radius"
version = "0.1.0"
description = "Stability radii for power systems"
authors = ["Ygor"]
license = "MIT"
readme = "README.md"
packages = [{ include = "stability_radius", from = "src" }]

[tool.poetry.dependencies]
python = "^3.10"
pandapower = "^2.13"          
numpy = "^1.26"
scipy = "^1.11"
pandas = "^2.2"
matplotlib = "^3.8"
hydra-core = "^1.3"           
requests = "^2.32.5"

[tool.poetry.group.dev.dependencies]
pytest = "^8.0"
pytest-cov = "^6.0"
ruff = "^0.11.0"
matpowercaseframes = "^2.0.1"
autograd = "^1.8"

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
target-version = "py310"
line-length = 88

[tool.coverage.run]
omit = [
    "src/stability_radius/application/cli.py",
    "src/stability_radius/verification/generate_report.py",
    "src/stability_radius/workflows.py",
]

[tool.coverage.report]
fail_under = 70

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
